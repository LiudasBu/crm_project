<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
        <title>Order {{ order.id }}</title>
        <style>
            li { 
                margin-bottom: 10px;  
            }
            * {
            font-family: "DejaVu Sans" ;
            font-size: 15px;
            }

        </style>
    </head>
    <body>
        <h4>VIKO CRM<br>Order number #{{ order.id }}</h4>
        <h4>Client: {{ order.client.name }}</h4>
        <h6>{{ order.client.address }} <br>
        {{ order.client.phoneNumber }}
        </h6>
        <h5>Products:</h5>
        <ul>
        {% set total_sum = 0 %}
            {% for product in order.products %}
                <li>
                    Name: <b>{{ product.name }}</b>
                    <br>
                    Unit price: {{ product.price }}€
                    <br>
                    Amount: {{ amount[product.id] }}
                    <br>
                    Total price: <b>{{ amount[product.id] * product.price }}€</b>
                    {% set total_sum =  total_sum + (amount[product.id] * product.price) %}
                </li>
            {% endfor %}
        </ul>
        <h4>Total price: {{ total_sum }}€</h4>

        <p>Date: {{ 'now'|date("Y-m-d")  }}</p>
    </body>
</html>